import { test, expect } from 'vitest'

test('Frontend test suite setup verification', () => {
  expect(1 + 1).toBe(2)
  expect('test').toBe('test')
  console.log('✅ Frontend test environment is working')
})
